Module ecuacion
    Sub main()
        'Dim a, b, c As Integer
        'a = 1
        'b = 5
        'c = 2
        'Console.WriteLine("El resultado de la ecuacion ({1}^2-4{0}*{2})/2a es: " & ((b ^ 2 - 4 * a * c) / 2 * a), a, b, c)
        Console.WriteLine("El resultado de la ecuacion (b^2-4ac)/2a donde a=1, b=5 y c=2 es: " & (5 ^ 2 - 4 * 1 * 2) / 2 * 1)
        Console.ReadKey()
    End Sub
End Module
